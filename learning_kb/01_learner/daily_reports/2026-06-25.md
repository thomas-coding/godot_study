# 2026-06-25 Daily Report

## Learner Line

- Completed Lesson 19: enemy and event linkage with wave spawning and gate unlock.
- Extended the Lesson 18 event system with a new `spawn_wave` action branch in `projects/first-game/scenes/main.gd`.
- Added `wave_configs`, `wave_started`, and `wave_cleared` state for restart-safe wave control.
- Added `Main/WaveEventTrigger` in `projects/first-game/scenes/main.tscn`:
  - `Event Id = wave_01`
  - `Target Action = spawn_wave`
  - `Trigger Once = On`
- Added `Main/WaveGate(StaticBody2D)` as the clear-wave gate target.
- Runtime-spawned enemies now join the `enemies` group and connect their damage/projectile/exit signals after `instantiate()`.
- Added clear-wave detection through `tree_exited` plus deferred group-count checking.
- Updated `projects/first-game/scenes/enemy.gd` so enemies:
  - turn around when colliding with a wall/gate,
  - can be defeated by the Player landing on them from above.

## Validation

- `WaveEventTrigger` spawns exactly 2 enemies for `wave_01`.
- Re-entering the trigger in the same run does not spawn duplicate waves.
- The first defeated wave enemy does not open the gate.
- Defeating the second wave enemy removes `WaveGate` and shows `Wave cleared: wave_01`.
- `R` reload restores the trigger, wave state, and gate.
- Existing collect-all Goal flow still works.
- Final regression passed with no red Debugger errors.

## Learner Understanding Confirmed

- `WaveGate` is saved as an initial node in `main.tscn`, not created by code.
- `wave_configs["wave_01"]["unlock_target"]` stores the target node name used for gate removal.
- `instantiate()` creates runtime enemies, while `add_child()` inserts them into the active scene.
- Runtime enemies need manual signal wiring because they were not present during `_ready()` child scanning.
- `tree_exited` plus `call_deferred()` is used to check wave clear after the scene tree/group state is stable.
- `call_deferred("_spawn_wave", event_id)` avoids changing collision/monitoring state during an `Area2D.body_entered` physics query.

## Next Lesson

- Lesson 20: small Boss prototype.
- Goal: build a Boss with HP, phase switching, hit feedback, and restart-stable state.
